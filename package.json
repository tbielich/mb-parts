{
  "name": "mb-parts-poc",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:data": "node scripts/build-data.mjs",
    "import:diagrams": "node scripts/import-diagram-mapping.mjs",
    "migrate": "tsc --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .tmp/migrate scripts/migrate-to-ndjson.ts && node .tmp/migrate/migrate-to-ndjson.js",
    "chunk:index": "tsc --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .tmp/migrate scripts/chunk-ndjson.ts && node .tmp/migrate/chunk-ndjson.js --vehicleKey default",
    "build": "npm run build:data && tsc && vite build",
    "preview": "vite preview",
    "generate:ikea-svg": "node scripts/generate-ikea-svg.mjs",
    "netlify:link": "npx -y netlify link",
    "netlify:build": "npx -y netlify build",
    "release:preview": "npx -y netlify deploy --build --json",
    "release:prod": "npx -y netlify deploy --prod --build --json"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
