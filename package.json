{
  "name": "mb-parts-poc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:parts": "node scripts/sync-parts.mjs",
    "sync:prices": "node scripts/sync-prices.mjs",
    "sync:all": "npm run sync:parts && npm run sync:prices",
    "build": "npm run sync:parts && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
